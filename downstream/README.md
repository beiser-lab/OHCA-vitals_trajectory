# Downstream Directory

This directory holds non-canonical materials that were previously mixed into the root of the repo.

Contents:
- `scripts/`: pooled-result builders, exploratory clustering, LCMM helpers, figure packs, and other extensions
- `abstracts/`: manuscript and abstract drafting files
- `patches/`: one-off local patch files

The canonical cross-site temperature pipeline remains at the repository root.
